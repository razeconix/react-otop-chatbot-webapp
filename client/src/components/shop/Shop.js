import React from 'react';
import ItemList from './itemList';

const Shop =() => (
    
  <div class="row" style={{paddingTop:30}}>
 <div class="container">
    <ItemList/>
    </div>
</div>
)

export default Shop;